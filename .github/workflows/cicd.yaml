name: CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:

    runs-on: ubuntu-latest

    steps:

    - name: Checkout
      uses: actions/checkout@v4

    - name: Login to DockerHub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build Docker Image
      run: |
        docker build -t username/myapp:${{ github.sha }} .

    - name: Push Docker Image
      run: |
        docker push username/myapp:${{ github.sha }}

    # - name: Clone GitOps Repo
    #   run: |
    #     git clone https://${{ secrets.GH_TOKEN }}@github.com/mohammedsulaimana9-git/GitOps-project-.git

    # - name: Update Image Tag
    #   run: |
    #     cd gitops-repo

    #     sed -i 's|image:.*|image: username/myapp:${{ github.sha }}|' deployment.yaml

    #     git config user.email "github-actions@github.com"
    #     git config user.name "github-actions-Runner"

    #     git add .
    #     git commit -m "Updated image tag"

        # git push

